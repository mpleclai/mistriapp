<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name = ".MistriappApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/core_ui_data_extraction_rules"
        android:fullBackupContent="@xml/core_ui_backup_rules"
        android:icon="@mipmap/core_ui_ic_launcher"
        android:label="@string/core_ui_app_name"
        android:roundIcon="@mipmap/core_ui_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/core_ui_theme_mistriapp"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/core_ui_theme_mistriapp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>